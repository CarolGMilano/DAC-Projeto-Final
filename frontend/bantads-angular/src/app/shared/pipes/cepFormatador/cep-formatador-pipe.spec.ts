import { CepFormatadorPipe } from './cep-formatador-pipe';

describe('CepFormatadorPipe', () => {
  it('create an instance', () => {
    const pipe = new CepFormatadorPipe();
    expect(pipe).toBeTruthy();
  });
});
