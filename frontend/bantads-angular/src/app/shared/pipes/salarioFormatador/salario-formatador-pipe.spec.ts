import { SalarioFormatadorPipe } from './salario-formatador-pipe';

describe('SalarioFormatadorPipe', () => {
  it('create an instance', () => {
    const pipe = new SalarioFormatadorPipe();
    expect(pipe).toBeTruthy();
  });
});
